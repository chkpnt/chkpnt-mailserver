- name: Install postfix, which provides a MUA (sendmail) and acts as the local MTA 
  zypper:
    name: postfix
    state: present

- name: Enable postfix and make sure it is running
  systemd:
    name: postfix
    enabled: yes
    state: started
