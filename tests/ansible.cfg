[defaults]
roles_path = testfixtures/roles
library = testfixtures/library
inventory = .vagrant/provisioners/ansible/inventory/vagrant_ansible_inventory
remote_user = vagrant
gathering = explicit
# Vagrant VMs are ephemeral
host_key_checking = false

[privilege_escalation]
become = false
